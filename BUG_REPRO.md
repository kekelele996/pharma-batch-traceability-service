# BUG_REPRO

## Bug 是什么
库存/入库列表接口用包级 scratch 切片做 s[:0]+append 复用底层数组：后一次过滤覆盖前一次返回、无过滤时返回上一次过滤的残留子集，过滤结果还会混入上一次查询内容。

## 如何触发
- listStock 先按批次过滤再不带过滤查询，返回残留子集而非全部行；
- listStock 先按批次过滤再按仓区过滤，返回混入上一次内容的错误结果；
- listInbound 先按状态过滤再不带过滤查询，同样返回残留子集。

## 错误信息
```
GET /api/v1/stock 期望 2 行，实际返回 1 行（上一次 batch 过滤的残留）
```
